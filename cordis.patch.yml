# dsh-opencode-models bundle patch: one host plugin row registering the four
# oc_model_* management tools. The browser half loads through this package's
# dsh.client manifest (an OpenCode Models settings section), not this patch.
- insert:
    - id: opencode-model-manager
      name: 'dsh-opencode-models'
